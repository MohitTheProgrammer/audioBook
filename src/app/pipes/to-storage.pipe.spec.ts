import { ToStoragePipe } from './to-storage.pipe';

describe('ToStoragePipe', () => {
  it('create an instance', () => {
    const pipe = new ToStoragePipe();
    expect(pipe).toBeTruthy();
  });
});
